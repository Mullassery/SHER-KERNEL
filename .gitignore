# Rust
/target/
# Cargo.lock is intentionally committed: this workspace produces a real
# binary (`sher-kernel`) and examples/benches, so the lockfile is tracked
# for reproducible builds (see CI, which restores/caches against it).
**/*.rs.bk
*.pdb

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Build artifacts
*.o
*.a
*.so
*.dylib

# OS
.DS_Store
Thumbs.db

# Project specific
*.log
debug/
